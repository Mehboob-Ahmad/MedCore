# Prompt 01 — Monorepo Setup

> AI prompt for setting up the MedicHp monorepo structure, tooling, and shared packages.

---

## Objective

Set up the MedicHp monorepo with proper project scaffolding, shared configurations, and development tooling.

## Tasks

1. Initialize the monorepo folder structure
2. Configure shared TypeScript settings in `packages/config/`
3. Set up ESLint and Prettier shared configs
4. Configure Docker Compose for local development
5. Create GitHub Actions CI pipeline
6. Write setup scripts in `scripts/`

## Constraints

- Do not initialize frameworks (Next.js, React, ASP.NET) yet
- Focus on tooling and configuration only
- Ensure all shared packages have proper `package.json` files

---

> **Status:** Placeholder — To be expanded with detailed instructions.
